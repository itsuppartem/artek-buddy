.env
data/
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.hypothesis/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
*.egg-info/
.cursor/sdk/

# secrets and packaged clients
client/token
client/.baked_token
*.deb

# local client bake (host URL / token) — never commit
client/url

# local agent handoff and Cursor rules — never commit
.cursor/handoff/
.cursor/rules/

# local-only tooling — never commit
scripts/
test-results/
docs/
docker-compose.local.yml
docker-compose.override.yml
workspace/*.py

# desktop UI toolchain
client/web/node_modules/
client/web/dist/

# host-side Cursor leftover; the live agent prompt is workspace/AGENTS.md
/AGENTS.md

# Playwright source tape; README ships media/demo.mp4
media/*.webm
