---
description: The old test suite is gone. Do not revive it.
alwaysApply: true
---

# Tests

The previous host, Vitest, Playwright, integration, and demo recorders were removed. They will be written from scratch. Do not add back `tests/run_*.py`, `make test-ui`, or `make demo`.

Do not point any new runner at the live `:8080` stack or the owner Postgres.
