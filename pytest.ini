[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: installed .deb against an ephemeral compose stack
    model: live Cursor/Grok turn (needs CURSOR_API_KEY on the runner)
filterwarnings =
    ignore::DeprecationWarning
addopts = -q --tb=short
